# ============================================================
# 项目级 npm 配置
# 注意：以下 electron_* 等 key 虽然新版 npm 会提示 "Unknown project config"，
# 但它们是 @electron/get、electron-builder 等工具识别的合法配置，
# 用于从国内镜像下载二进制文件，警告可以忽略。
# ============================================================

# npm registry - 使用淘宝镜像
registry=https://registry.npmmirror.com

# ---- Electron 生态下载镜像（核心配置，解决国内无法下载 GitHub Releases 的问题）----
electron_mirror=https://npmmirror.com/mirrors/electron/
electron_builder_binaries_mirror=https://npmmirror.com/mirrors/electron-builder-binaries/

# ---- 其他常用二进制镜像（可选，用于将来扩展）----
playwright_download_host=https://npmmirror.com/mirrors/playwright/
chromedriver_cdnurl=https://npmmirror.com/mirrors/chromedriver
operadriver_cdnurl=https://npmmirror.com/mirrors/operadriver
phantomjs_cdnurl=https://npmmirror.com/mirrors/phantomjs
node_inspector_cdnurl=https://npmmirror.com/mirrors/node-inspector
fsevents_binary_host_mirror=https://npmmirror.com/mirrors/fsevents/
